@model IEnumerable<Category>
    <div class="my-3">
        <ul class="list-group">
            @foreach (Category cat in Model)
            {
                <li class="list-group-item">
                    @cat.CategoryName </li>
            }
        </ul>
    </div>

 